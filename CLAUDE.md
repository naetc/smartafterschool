# 이 저장소에서 작업할 때

초등 방과후학교 지원금 정산 시스템. 빌드 없는 정적 웹앱이고, **학생 개인의 돈이 걸린 회계 프로그램**이다. 여기서 틀린 숫자는 행정실에 제출되고 학부모에게 청구된다.

구조는 [README.md](README.md), 계산 규칙은 [core-rules.md](core-rules.md)(헌법 제1~7조)를 볼 것.

---

## 검증 3원칙

2026-09-17 점검에서 실제 금액이 틀어지는 결함 5건을 찾았다. 그때 기존 회귀 테스트 62건은 **전부 통과**하고 있었고 실제 운영 백업의 총액도 멀쩡했다. 코드를 읽어서 찾은 게 아니라 아래 방법으로 찾았다. 점검·수정 작업에는 이 세 가지를 지킬 것.

### 1. 눈으로 훑지 말고 검증 도구를 만들어라

의심스러운 곳을 짚는 방식으로는 못 찾는다. 사람이 미리 생각해낸 경우만 보게 되기 때문이다.

**"금액이 얼마인가"가 아니라 "어떤 경우에도 반드시 참인 성질"을 검사한다.** 정답을 몰라도 되니 수천 건을 돌릴 수 있고, 상상하지 못한 조합이 걸린다.

```
기존 테스트  : "이 입력엔 60,000원이 나와야 한다"   ← 정답을 알아야 씀
불변식       : "초3 + 자유 + 자부담 = 청구액"        ← 정답을 몰라도 씀
```

이미 만들어져 있다 — 새로 만들지 말고 여기에 불변식을 추가할 것.

```bash
npm run fuzz -- 5000                  # 무작위 시나리오 (불변식 A1~A10)
node test/fuzz-engine.js --seed 116   # 위반 seed를 그대로 재현
```

엔진([app-engine.js](app-engine.js))이나 [app-core.js](app-core.js)의 `commitState`를 손댔으면 `npm test`만으로 끝내지 말 것.

### 2. 그 테스트가 수정 전 코드에서도 통과하는지 확인하라

**통과만 하는 테스트는 아무것도 지키지 않는다.** 새 테스트를 썼으면 수정 전 커밋으로 반드시 역검증한다.

```bash
git show <수정전커밋>:app-engine.js > /tmp/before/app-engine.js   # 소스만 갈아끼워 실행
```

2026-09-17에 이걸로 실제 사고를 두 번 막았다.

- 결함2 테스트가 구버전에서도 통과 → 테스트 헬퍼가 `commitState`를 직접 흉내내는 바람에 버그를 가리고 있었다. 실제 `window.commitState`를 부르도록 고쳐야 판별됐다.
- 퍼즈 A10이 477건 실패 → **앱 버그가 아니라 테스트 하네스 버그**였다. 퍼즈 시나리오가 앱과 다른 재연산 경로를 타고 있었다.

자기 결과부터 의심할 것. 실패가 떴다고 곧장 앱을 고치지 말고, 테스트가 맞는지 먼저 본다.

### 3. 실제 운영 백업으로 총액을 대조하라

"고쳤다"가 아니라 **"기존 장부가 안 바뀐다"**를 증명해야 한다. 이 시스템은 껍데기만 배포하고 장부는 각자 브라우저에 있어서(헌법 제7조), 엔진을 고치면 **이미 제출한 숫자가 조용히 달라질 수 있다.**

저장소 루트의 `방과후정산_백업_*.json`(gitignore 대상, 개인정보 포함)을 불러 초3·자유·자부담 **총액이 1원도 안 바뀌는지** 확인한다. 바뀐다면 그게 의도한 것인지 먼저 사람에게 묻는다.

바뀌어야 맞는 수정이라면 `window.BASELINE_VER`를 올리고 주석에 이유를 남긴다.

> ⚠ 이 백업 파일들은 **실명·학적이 담긴 개인정보**다. 내용을 화면에 출력하거나 저장소에 커밋하지 말 것. 총액·건수 같은 집계만 쓴다.

---

## 협업 방식

사용자는 스스로를 초보 개발자라고 하지만, **회계 규칙과 실무 맥락은 사용자만 안다.** 코드에는 "무엇을 하는지"만 있고 "왜 그래야 하는지"는 없다.

- 요청을 그대로 실행하기 전에 **대안·리스크를 먼저 설명하고 물어볼 것**. 특히 새 의존성 추가, 배포된 파일 통째 재생성, 되돌리기 어려운 작업.
- 계산 규칙(헌법 제1~7조)을 건드리는 변경은 **코드를 쓰기 전에 보고하고 허락을 구할 것.**
- 설계 의도가 불분명하면 추측하지 말고 물어볼 것. 2026-09-17의 조정/환불 구분(제6조)은 사용자가 설명해주기 전까지 코드만 봐서는 알 수 없는 내용이었다.

배경은 [Claude_작업역할메모.txt](Claude_작업역할메모.txt) 참고.

---

## 배포

원격이 둘이고 **역할이 다르다.**

| 원격 | 역할 |
| --- | --- |
| `origin` | 소스·개발 저장소 (GitHub Pages 없음) |
| `smartafterschool` | **실제 배포처** — GitHub Pages, 여기 push하는 순간 실서비스에 나간다 |

**모든 커밋은 양쪽에 push**한다. 갈라졌으면 force가 아니라 merge.

앱 코드(`app-*.js`, `index.html`, `style.css`, `updates.js`)를 고쳤으면 [index.html](index.html)의 `?v=` 버전을 올린다(형식 `YYMMDD` + 리비전 문자). 캐시 때문에 안 올리면 사용자가 옛 파일을 계속 받는다. 테스트·문서만 고쳤으면 올리지 않는다.

기능을 배포할 때는 [updates.js](updates.js)에 공지를 한 줄 추가한다. 사용자가 시작 모달에서 본다.
