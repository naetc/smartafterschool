### **📜 방과후 스마트 정산 시스템 개발 헌법 (core-rules.md)**

**\[AI 프롬프트 지시어\]**  
⚠️ **경고:** 본 문서는 시스템의 핵심 연산 논리를 규정한 '절대 불변의 헌법'이다. AI는 UI, 기능 추가, 버그 수정 등 어떠한 코드를 작성할 때에도 아래의 원칙을 단 하나라도 위배해서는 안 되며, 위배될 소지가 있는 경우 코드 작성 전 사용자에게 논리적 모순을 먼저 보고하고 허락을 구해야 한다.

#### **제1조. 주머니 이론 (The Pocket Theory)**

정산 연산의 본질은 '학생의 큰 주머니(지원금 예산)'에서 '강좌의 작은 주머니(항목별 청구액)'로 돈을 흘려보내는 과정이다.

1. **예산(큰 주머니):** 초3 지원금(25만 원)과 자유수강권(60만 원)은 학생 단위의 통합 예산이다. 강좌별로 예산이 쪼개져 있지 않다.  
2. **타겟(작은 주머니):** 수강료(T), 교재비(B), 재료비(M)는 각각 독립된 청구 항목이다.  
3. **우선순위 차감:** 예산은 환경설정(Global) 또는 개별 강좌 설정(Local)에 명시된 순서대로 작은 주머니를 0원으로 만들 때까지 폭포수처럼 차감된다.

#### **제2조. \[핵심\] 분기 선 차감, 후 차수 안분 (Deduction First, Distribution Later)**

가장 빈번하게 발생하는 연산 오류를 방지하기 위한 최우선 원칙이다.

1. **계산은 숲을 본다:** 지원금 차감 연산은 **반드시 '분기 총액'을 기준**으로 먼저 모두 끝마쳐야 한다. (예: 분기 총 수강료 12만 원, 총 교재비 4만 원을 둔 상태에서 룰에 따라 지원금을 모두 뺀다.)  
2. **쪼개기는 마지막에:** 총액 단위의 공제 연산이 완벽히 끝난(u\_tc, u\_bc, u\_mc가 확정된) 후에야, 비로소 그 결과값을 1차수, 2차수, 3차수(Session)의 크기에 맞춰 안분(Distribution)하여 화면에 뿌려준다.  
3. **금지 사항:** 절대 1차수, 2차수 루프(Loop) 안에서 지원금 잔액을 차감하는 로직을 짜지 마라. 차수별 시야 협착으로 인해 전체 우선순위가 붕괴된다.

#### **제3조. 개별 공제 룰의 최우선 독립성 (Absolute Respect for Local Override)**

글로벌 룰보다 강좌별 개별 룰(Local Override)이 무조건 우선한다.

1. **항목 우선(ITEM\_FIRST) 모드에서의 동작:** 글로벌 룰이 '교재비(B)'를 먼저 차감하는 스텝이더라도, 특정 강좌(A)에 '수강료(T) 우선'이라는 개별 룰이 걸려 있다면, **해당 스텝에서 A강좌는 남들 눈치를 보지 않고 당당히 자신의 1순위인 '수강료(T)' 주머니를 내밀어 동시에 차감**받아야 한다.  
2. **금지 사항:** 글로벌 룰의 순서에 개별 강좌의 순서를 억지로 종속시키지 마라.

#### **제3조의2. '초3 → 자유' 순서의 유일한 예외 (육아기 근로시간 단축)**

제1조 3항의 우선순위 차감은 원칙적으로 항상 **초3지원금 → 자유수강권** 순서다. 이 순서를 뒤집는 예외는 시스템 전체에서 **단 하나, 자유수강권 사유(reason)가 `CHILDCARE_REDUCED`(육아기 근로시간 단축)로 지정된 경우뿐**이다.

1. **역전 조건:** 학생의 자유수강권 등록에 `reason === 'CHILDCARE_REDUCED'`가 설정돼 있으면, 그 학생은 **자유수강권을 초3지원금보다 먼저** 소진한다 ([app-engine.js](app-engine.js) 참고).
2. **지원기간 경계:** 이 구분은 `endQ`/`endSess`(지원 종료 분기·차수)를 함께 가질 수 있고, 종료 시점 이후 구간은 역전이 풀려 원래 순서(초3 먼저)로 되돌아간다.
3. **금지 사항:** 새로운 사유(reason)나 예외 케이스를 추가할 때, 이 예외를 다른 사유로 일반화하거나 이 조건 검사 없이 순서를 뒤집는 코드를 다른 경로에 추가하지 마라. '초3 → 자유' 순서를 깨는 것은 항상 이 하나의 명시적 조건을 통해서만 허용된다.

#### **제4조. 데이터 흐름과 재연산 트리거 (State & Trigger)**

UI 화면과 엔진의 데이터 흐름은 단방향으로 톱니바퀴처럼 맞물려야 한다.

1. UI(학생 콘솔 등)에서 사용자가 설정값을 변경(e.overrideCho3 등)하면, 시스템은 그 즉시 window.commitState로 데이터를 DB에 밀어 넣어야 한다.  
2. 상태 변경 직후에는 **반드시 window.autoRunSet(true)를 호출**하여, 변경된 설정값을 엔진이 강제로 다시 읽어들여 전체 정산액을 실시간으로 재계산하고 화면을 갱신하도록 트리거를 당겨야 한다.


#### **제5조. 확정된 회계는 고치지 않는다 — 한도 초과는 '감지·경고'만 (2026-09-17 신설)**

마감(`SysSet.closedSess`)된 차수와 환불로 동결(`e.frozenSplit`)된 강좌의 금액은 **이미 확정된 회계**다. 엔진은 이 값을 그대로 재생하며, 나중에 데이터가 바뀌어도 소급해서 고치지 않는다.

1. **한도 초과 가능성:** 확정 이후에 강좌 요금표나 조정이 바뀌면, 그 확정 금액의 합계가 학생의 연간·상반기 한도를 넘길 수 있다.
2. **금지 사항:** 이때 시스템이 임의로 초과분을 잘라내지 마라. 이미 결재가 올라간 분기의 숫자가 사람 모르게 바뀌는 것이 과다 청구보다 위험하다.
3. **해야 할 일:** `window.getBudgetOverruns()`로 감지해서 4스텝 화면 상단 배너로 **사람에게 알리기만** 한다. 자를지, 마감을 해제하고 다시 계산할지는 사람이 판단한다.
4. **거짓 경보 금지:** 실제로 넘지 않았는데 경고가 뜨면 사용자가 경고 자체를 무시하게 된다. 두 방향 모두 [test/fuzz-engine.js](test/fuzz-engine.js)의 A6·A6b 불변식이 지킨다.

#### **제6조. 조정과 환불은 다른 사건이다 (2026-09-17 신설)**

정산(행정실 제출)이라는 행위를 기준으로, **조정(adjust)은 그 앞 단계**이고 **환불(refund)은 그 뒤 단계**다. 원칙적으로 조정은 정산 전에 모두 끝내야 하지만, 실무에서는 실수나 변수로 나중에 하게 될 수 있다. 그래도 결과가 달라져서는 안 된다.

1. **환불 = 정산 후의 사건 → 격리한다.** 이미 제출한 다른 강좌·다른 학생의 금액이 환불 하나 때문에 바뀌면 안 된다. `baseline`/`frozenSplit` 스냅샷이 이 격리를 담당한다(제5조 참고).
2. **조정 = 정산 자체의 정정 → 지원금에서 처리한다.** 조정으로 늘어난 금액은 지원금 잔액이 남아 있는 한 지원금이 부담해야 한다. 그래서 `locked_t*`에는 `frozenSplit` 전액이 아니라 **자부담 몫(finT/finB/finM)만** 넣어, 조정분이 워터폴에 정상 재진입하게 한다.
3. **환불로 아낀 지원금은 다음 분기로 이월한다.** 조정으로 줄어든 금액은 이월 대상이 **아니다** — 그건 애초에 정산이 잘못됐던 것이므로 같은 분기에서 바로잡힌다.
4. **마지막(4)분기의 환불 지원금은 소멸한다.** 이월할 다음 분기가 없기 때문이며, 이는 의도된 동작이다. 화면에는 '이월'이 아니라 '소멸'로 안내해야 한다.
5. **[핵심] 경로 동등성:** 위 규칙들의 결과로, **조정을 언제 했든 최종 금액은 같아야 한다.**
   `조정 → 정산 → 환불` 과 `정산 → 환불 → 조정` 의 최종 3분할이 1원도 달라선 안 된다.
   이를 위해 `baseline`은 "찍힌 시점의 화면값"이 아니라 **"이 강좌에 환불이 없었다면 받았을 3분할"** 로 정의되며, 조정·요금표가 바뀌면 `recaptureBaseline()`이 다시 계산한다.
6. **금지 사항:** 조정과 환불을 같은 경로로 처리하지 마라. 둘을 `recalcEnrollment`의 결과 하나로 뭉뚱그려 `frozenSplit`에서 함께 벗겨내면, 조정분이 '환불로 아낀 돈'으로 잘못 분류되어 이월된다(2026-09-17 이전의 실제 버그 — 2,000건 중 72%에서 금액이 달랐다).
7. 이 조항은 [test/fuzz-engine.js](test/fuzz-engine.js)의 **A9(경로 동등성)** 불변식과 [test/regression-2026-09-17.test.js](test/regression-2026-09-17.test.js)가 지킨다.

#### **제7조. 계산 결과가 말없이 달라져서는 안 된다 (2026-09-17 신설)**

이 시스템은 껍데기(HTML/JS)만 웹에 올려두고 장부는 각자 브라우저(IndexedDB)와 백업 JSON에 둔다. 그래서 사용자가 접속할 때마다 **그 순간 배포돼 있는 엔진**으로 장부를 처음부터 다시 계산한다. 화면의 금액은 저장된 값이 아니라 매번 새로 유도되는 값이다.

1. **위험:** 원자료(C/M/F/E/SysSet)를 하나도 안 건드려도, 엔진이 바뀌면 어제 본 금액과 오늘 본 금액이 달라질 수 있다. 이미 행정실에 제출한 숫자와 화면 숫자가 어긋나 있어도 사용자는 알 방법이 없다.
2. **해야 할 일:** 저장할 때마다 "사용자가 방금 본 계산 결과"를 지문(`lastComputed`)으로 함께 저장하고, 앱을 열 때 현재 엔진의 결과와 대조한다. 금액이 달라졌으면 **모달로 무엇이 얼마나 바뀌었는지 보고**하고 엑셀로 받을 수 있게 한다.
   - 이 모달은 업데이트 소식()과 금액 영향을 **한 창에서** 보여준다. 둘이 흩어져 있으면 정작 중요한 금액 변동을 놓친다(그래서 2026-09-17에 흐르는 띠 배너를 없애고 통합했다).
   - **자동 노출은 "마지막으로 본 버전 ≠ 현재 버전"일 때 1회뿐이다.** 접속할 때마다 띄우면 안 된다 — 공지는 기본 14일 노출이라 평상시에도 여러 건 쌓여 있고, 그러면 창이 매일 뜨는데 금액 칸은 대개 "변경 사항 없습니다"가 되어 사용자가 확인 버튼을 반사적으로 누르게 된다(3항과 같은 이유).
   - **금액이 달라졌을 때만** X·ESC·바깥클릭을 막고 [확인했습니다]로만 닫히게 한다. 그 버튼을 누르는 것이 곧 "새 금액을 기준으로 삼는다"는 처리다.
3. **버전 비교 금지:** 버전 번호만 보고 경고하지 마라. 업데이트 대부분은 화면만 바뀌는데 그때마다 경고가 뜨면 사람이 경고 자체를 무시하게 된다. **실제 금액이 달라졌을 때만** 알린다.
4. **거짓 경보 금지:** 사용자가 직접 데이터를 고친 경우는 저장 시점에 지문도 함께 갱신되므로 경고가 뜨지 않는다. 새 경로를 추가할 때 이 성질(`refreshComputedFingerprint`가 저장 직전에 호출되는 것)을 깨지 마라.
5. **가장 확실한 예방은 마감이다.** 마감(`closedSess`)된 차수는 스냅샷을 재생하므로 엔진이 바뀌어도 금액이 고정된다(제5조). 제출한 분기는 마감하도록 안내하고, 이 감지 기능은 그 그물을 빠져나간 것을 잡는 **안전망**으로 둔다.
6. **차감 규칙을 바꿀 때는** `BASELINE_VER`를 올리고, 실제 운영 백업으로 총액이 바뀌는지 먼저 대조할 것.
